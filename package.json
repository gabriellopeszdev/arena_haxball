{
  "name": "arena-vincere",
  "version": "2.0.0",
  "description": "Sistema de salas HaxBall com Discord bot, GIF clipping e multi-salas",
  "main": "src/index.ts",
  "scripts": {
    "start": "ts-node src/index.ts",
    "dev": "ts-node src/index.ts",
    "build": "tsc",
    "lint": "tsc --noEmit",
    "postinstall": "node scripts/patch-haxball.js",
    "sync": "rsync -avz --delete --exclude node_modules --exclude .git --exclude clips --exclude data.db* --exclude .env --exclude .env.example --exclude README.md --exclude dist ./ $VPS_USER@$VPS_HOST:/home/$VPS_USER/vincere",
    "sync:full": "npm run sync && ssh $VPS_USER@$VPS_HOST 'cd /home/$VPS_USER/vincere && npm install && pm2 restart all'"
  },
  "keywords": [
    "haxball"
  ],
  "author": "Fusion",
  "license": "ISC",
  "dependencies": {
    "better-sqlite3": "^11.7.0",
    "discord.js": "^14.18.0",
    "dotenv": "^16.4.7",
    "form-data": "^4.0.2",
    "haxball-extended-room": "^3.0.0",
    "haxball.js": "^2.3.3",
    "puppeteer": "^24.0.0",
    "undici": "^7.5.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.12",
    "@types/node": "^22.0.0",
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.7.0"
  }
}
