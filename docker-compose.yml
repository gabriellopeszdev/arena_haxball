services:
  arena:
    build: .
    container_name: arena_haxball
    restart: unless-stopped
    env_file: .env
    environment:
      - CHROMIUM_PATH=/usr/bin/chromium
      - NODE_OPTIONS=--max-old-space-size=4096
    volumes:
      - ./data.db:/app/data.db
      - ./clips:/app/clips
    shm_size: 256mb
    cpu_shares: 2048
    cpus: "1.5"
    mem_limit: 5g
    mem_reservation: 2g
